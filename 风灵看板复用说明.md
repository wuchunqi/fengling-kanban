# 风灵看板完整工具包 — 复用部署说明

整合 **短期班**（四学部日报 + 周维度）与 **长期班**（日/周维度 + 明细）的完整工具包。从源 Excel 自动生成网页看板，支持一键发布到 GitHub Pages。

---

## 一、工具包内容

| 文件/目录 | 说明 |
|-----------|------|
| `generate_daily_reports.py` | 短期班：生成各学部 3 类 Excel 日报 |
| `build_daily_web_dashboard.py` | 短期班：生成日/周维度网页看板 |
| `build_changqi_dashboard.py` | 长期班：生成日/周维度 + 明细看板 |
| `update_and_publish.sh` | 一键：短期班 + 长期班 全部更新 + git 推送 |
| `一键更新并发布.command` | macOS 双击运行 |
| `templates/` | Excel 样式模板 |
| `每日输出/` | 各学部样例输出（可作套版参考） |
| `index.html` | 看板总入口（短期班 + 长期班） |
| `config.env.example` | 配置样例 |

### 短期班看板页面

| 页面 | 说明 |
|------|------|
| `每日三表汇总看板.html` | 日维度总看板（6 个学部） |
| `周维度在线率看板.html` | 周维度入口 + 折线图 |
| `每日三表汇总看板_详情/` | 授权/销售/不在线明细 |

### 长期班看板页面

| 页面 | 说明 |
|------|------|
| `长期班风灵在线看板.html` | 日维度（初中/高中/小学 × 学科） |
| `长期班周维度在线率看板.html` | 周维度（周四～周一，折线图 + 分学科汇总） |
| `长期班风灵在线看板_详情/detail.html` | 不在线时段明细 |

---

## 二、环境准备（首次）

```bash
python3 --version   # 需 3.9+
pip3 install -r requirements.txt
cp config.env.example config.env   # 编辑 PAGES_URL 等
```

---

## 三、GitHub Pages 首次部署

```bash
cd /path/to/风灵看板完整工具包
git init
git add .
git commit -m "init fengling dashboard kit"
git branch -M main
git remote add origin https://github.com/你的用户名/fengling-dashboard.git
git push -u origin main
```

仓库 → **Settings** → **Pages** → Branch 选 `main`，文件夹 `/ (root)`

固定地址：`https://你的用户名.github.io/fengling-dashboard/`

---

## 四、每日更新流程（SOP）

### 1. 下载源文件到「下载」文件夹

**短期班（4 份）：**

| 文件名前缀 | 说明 |
|-----------|------|
| `销售风灵在线率明细数据_` | 销售在线率 |
| `爱芯个微授权数据_` | 高中授权（不含「爱学」） |
| `风灵个微在线数据_` | 个微在线 |
| `爱芯个微授权数据_爱学_` | 爱学授权 |

**长期班（1 份）：**

| 文件名前缀 | 说明 |
|-----------|------|
| `长期班辅导风灵在线明细数据_` | 长期班在线明细 |

### 2. 执行更新

双击 `一键更新并发布.command`，或：

```bash
chmod +x update_and_publish.sh
./update_and_publish.sh
```

### 3. 脚本自动完成

1. 识别 Downloads 最新源文件
2. 生成短期班 Excel → `每日输出/`
3. 生成短期班 + 长期班全部 HTML 看板
4. `git commit` + `push`

### 4. 查看结果

- 总入口：`index.html` 或 GitHub Pages 根地址
- 强制刷新：`Cmd + Shift + R`

---

## 五、口径说明

### 短期班分组

| 条件 | 分组 |
|------|------|
| 运营中心 = 郑州一部 | 初短一部 |
| 运营中心 = 郑州二部 | 初短二部 |
| 运营中心 = 郑州三部 | 初短三部 |
| 运营中心 = 郑州特战队 | 郑州特战队 |
| 运营中心 = 郑州 且 年级 = 全年级 | 小短 |
| 其余 运营中心 = 郑州 | 高短 |

### 长期班

- 学部：初中 / 高中 / 小学（源表 `学段` 列）
- 学科：英语、语文、数学、物理、化学
- 周维度：仅统计 **周四～周一**，不含周二、周三
- 达标：在线率 ≥ 99.9%

---

## 六、仅本地生成（不发布）

**短期班：**

```bash
python3 generate_daily_reports.py \
  --sales "$HOME/Downloads/销售风灵在线率明细数据_xxx.xlsx" \
  --auth-high "$HOME/Downloads/爱芯个微授权数据_xxx.xlsx" \
  --wechat "$HOME/Downloads/风灵个微在线数据_xxx.xlsx" \
  --auth-aixue "$HOME/Downloads/爱芯个微授权数据_爱学_xxx.xlsx"

python3 build_daily_web_dashboard.py
```

**长期班：**

```bash
python3 build_changqi_dashboard.py \
  --source "$HOME/Downloads/长期班辅导风灵在线明细数据_xxx.xlsx"
```

---

## 七、常见问题

1. **看板日期不对** → 由源 Excel 业务日期决定，需下载最新业务日数据
2. **git push 失败** → `git -c http.version=HTTP/1.1 push origin main`
3. **样式不对** → 确认 `templates/样式表.xlsx` 和 `templates/样式表2.xlsx` 存在
4. **长期班折线图空白** → 需联网加载 Chart.js CDN

---

**版本**：2026-09-10  
**适用**：郑州风灵短期班 + 长期班完整看板
