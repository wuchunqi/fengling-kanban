# 风灵日报每日处理流程（固定口径）

> 该文档为“今日已验收通过”的最终标准流程。后续每日按此执行，不再随意改口径或改样式。

## 1) 输入文件（每日4份）

- `销售风灵在线率明细数据_*.xlsx`
- `爱芯个微授权数据_*.xlsx`（高中）
- `风灵个微在线数据_*.xlsx`
- `爱芯个微授权数据_爱学_*.xlsx`

## 2) 分组规则（当前生效）

- `运营中心=石家庄一部` → `初短一部`
- `运营中心=石家庄二部` → `初短二部`
- `运营中心=石家庄 且 年级=全年级` → `小短`
- 其余 `运营中心=石家庄` → `高短`

## 3) 关键数据口径（当前生效）

- 不按 `小时=23` 过滤授权数据，直接使用用户提供的授权明细数据。
- 战队名做标准化（`--` 统一为 `-`）避免同战队重复。
- 接流人数统一口径：授权去重人数、个微去重人数、销售去重人数三者取最大值。
- 强约束：
  - `全天在线 <= 低价课带班`
  - `电脑端在线人数 <= 接流人数`
  - `手机端在线人数 <= 接流人数`
  - 所有在线率/授权率/正常率 `<= 1`
- 学部判定补充：
  - `新兵营`按来源判定：`sys_source=爱学` 归初中，`sys_source=高中` 归高中。

## 4) 输出目录

- 根目录：`/Users/zoeyzhou/Desktop/工作/风灵数据/每日输出`
- 子目录：`初短一部`、`初短二部`、`小短`、`高短`
- 每个子目录3个文件：
  - `石家庄-<分组>-风灵个微全天在线率&爱芯后台授权.xlsx`
  - `石家庄-<分组>-销售风灵在线率明细数据.xlsx`
  - `石家庄-<分组>-每日风灵不在线.xlsx`

## 5) 执行脚本

- 主脚本：`/Users/zoeyzhou/Desktop/工作/风灵数据/generate_daily_reports.py`
- 常用执行示例：

```bash
python3 "/Users/zoeyzhou/Desktop/工作/风灵数据/generate_daily_reports.py" \
  --sales "/path/销售风灵在线率明细数据.xlsx" \
  --auth-high "/path/爱芯个微授权数据_高中.xlsx" \
  --wechat "/path/风灵个微在线数据.xlsx" \
  --auth-aixue "/path/爱芯个微授权数据_爱学.xlsx" \
  --output-root "/Users/zoeyzhou/Desktop/工作/风灵数据/每日输出"
```

## 6) 最终样式标准（已验收）

### 6.1 数据公示表（`风灵个微全天在线率&爱芯后台授权.xlsx`）

- 字体：全表统一“苹方-简”（PingFang SC）。
- 汇总行：全部加粗。
- 学部列：从首条数据行合并到最终“学部汇总”行（含该行）。
- 最终学部汇总行：`B:C` 合并，写 `<学部> 汇总`，橙色加粗。
- 在线率显示：百分比无小数（`0%`）。
- 在线率阈值字体：
  - `<60%` 红色
  - `>90%` 绿色
  - 其余黑色
- 数据条：
  - `个微全天在线率`：淡紫
  - `爱芯个微授权率`：淡橙
  - `个微功能正常率`：淡绿
- 最终汇总行下一行：`A:K` 淡紫底色条。

### 6.2 战队数据（`销售风灵在线率明细数据.xlsx` 的 `战队数据`）

- 学部列：加宽，合并从首条战队数据到最终学部汇总行，居中加粗。
- 年级列：连续相同年级合并，居中。
- 最终学部汇总行：`B:C` 合并，写 `<学部> 汇总`，橙色加粗。
- 汇总行：加粗。
- 在线率显示：百分比无小数（`0%`）。
- 在线率阈值字体：
  - `<60%` 红色
  - `>90%` 绿色
  - 其余黑色
- 数据条：
  - 电脑端全天在线率：淡紫
  - 手机端全天在线率：橙黄
- 最终汇总行下一行：`A:H` 淡紫底色条。

## 7) 每日执行标准步骤（SOP）

1. 收到4个原始文件路径。
2. 执行脚本产出四个分组（先数据后样式）。
3. 自动校验数据约束：
   - 在线数不大于接流人数
   - 所有在线率不大于1
   - 分组学部是否符合规则
4. 套用最终样式标准（本文件第6节）。
5. 最后抽检每个分组至少1个文件：
   - 汇总行是否加粗
   - `B:C` 学部汇总是否合并
   - 字体是否“苹方-简”
6. 交付并提示用户刷新重开文件。

## 8) 变更管理规则

- 若用户提出新口径/新样式，先确认后更新本文件，再改脚本。
- 禁止在未记录变更的情况下临时修改逻辑，避免次日跑偏。

## 9) 每日默认执行约束（强制）

- 默认模式：**只改数据，不改格式**（除非用户明确要求改样式）。
- 每次执行后必须做数据复核，至少包含：
  - 在线人数不大于接流人数；
  - 所有在线率/授权率/正常率不大于1；
  - 四个分组学部与年级映射符合当日口径；
  - **高短专项复核**：不得出现初中字段，不得出现错位汇总行。
